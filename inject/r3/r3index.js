function copyNote() {
    navigator.clipboard.writeText(" ( monster ~ tentacle ~ bestiality ~ interspecies ~ ai_generated ~ anthro ~ fur ~ furry ~ slime ~ insects ) -futanari -male_only  -femboy -male/male ");
}


// Original button
function viewOriginal() {
    // Get src of image with id "image"
    const image = document.getElementById("image");
    const src = image.src;

    // Open the original image in the same tab
    window.location.href = src;
}
